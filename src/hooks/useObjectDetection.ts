// TDD RED Phase: Empty Implementation to satisfy TypeScript compiler
// The test will fail at runtime because logic is missing.

export const useObjectDetection = () => {
  return {
    isModelLoaded: false,
    detect: async () => []
  };
};
